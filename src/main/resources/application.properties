spring.application.name=CodeHarbour

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d92idjlaeets73el28jg-a.virginia-postgres.render.com:5432/codeharbour}
spring.datasource.username=${DATABASE_USERNAME:codeharbour_user}
spring.datasource.password=${git :}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

server.port=${PORT:8080}


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

razorpay.api.key=${RAZORPAY_API_KEY:}
razorpay.api.secret=${RAZORPAY_API_SECRET:}
